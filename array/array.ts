// First Representation
let myArr: string[] = [];
myArr.push("ironman");

// Second Representation
let meraArr: Array<string> = [];
meraArr.push("Superman");

// Another example
type User = {
  name: string;
  isActive: boolean;
};

let userArr: User[] = [];
